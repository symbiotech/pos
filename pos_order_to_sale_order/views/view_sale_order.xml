<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="view_sales_order_filter" model="ir.ui.view">
        <field
            name="name"
        >sale.order.list.select.inherit.pos_order_to_sale_order</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_sales_order_filter" />
        <field name="arch" type="xml">
            <field name="user_id" position="after">
                <field name="pos_session_id" />
            </field>
            <filter name="my_sale_orders_filter" position="after">
                <separator />
                <filter
                    name="from_pos"
                    string="From Point of Sale"
                    domain="[('pos_session_id', '!=', False)]"
                />
            </filter>
            <filter name="customer" position="after">
                <filter
                    name="pos_session"
                    string="PoS Session"
                    context="{'group_by': 'pos_session_id'}"
                />
            </filter>
        </field>
    </record>

    <record id="view_sale_order_tree" model="ir.ui.view">
        <field name="name">sale.order.list.inherit.pos_order_to_sale_order</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.sale_order_tree" />
        <field name="arch" type="xml">
            <field name="client_order_ref" position="after">
                <field name="pos_session_id" optional="hide" />
            </field>
        </field>
    </record>

    <record id="view_sale_order_form" model="ir.ui.view">
        <field name="name">sale.order.form.inherit.pos_order_to_sale_order</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form" />
        <field name="arch" type="xml">
            <field name="origin" position="after">
                <field name="pos_session_id" invisible="not pos_session_id" />
                <field
                    name="pos_order_uuid"
                    groups="base.group_no_one"
                    invisible="not pos_order_uuid"
                />
            </field>
        </field>
    </record>
</odoo>
